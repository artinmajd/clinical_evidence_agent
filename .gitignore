# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Environment / secrets
.env
*.local

# OS
.DS_Store

# Data (corpus can be large; keep it out of git, document how to regenerate)
data/raw/
data/processed/
*.db
*.sqlite3

# Personal working notes (not part of the portfolio code)
NOTES.md
PROGRESS.md

# Project challenges log (personal, not part of the portfolio code)
CHALLENGES.md
eval/last_run.json

# Terraform local state and downloaded provider binaries - state can contain
# resource details, providers are large and machine-specific; both are
# meant to be regenerated locally (state lives in this project only since
# we destroy the infra after each test, no remote backend needed).
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/crash.log
_stale_git_locks/
