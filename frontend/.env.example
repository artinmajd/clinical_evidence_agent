# Used only by `npm run dev` for local development - copy to .env and
# point it at whichever backend you're running locally (see the main
# project's Dockerfile / docker run instructions).
VITE_API_URL=http://localhost:8000
